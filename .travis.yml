language: c

compiler:
  - gcc

before_install:

script: make && sudo make install && cd t && make DEfAULT_TEST_TARGET=tap

## whitelist
branches:
  only:
    - master

notifications:
  email:
    -  albertofanjul@gmail.com
